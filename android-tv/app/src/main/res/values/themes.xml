<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Fullscreen black theme for the kiosk activity. -->
    <style name="Theme.TvKiosk" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
        <item name="android:windowContentTransitions">false</item>
    </style>

    <!-- Config activity gets a normal (non-fullscreen) theme so the soft
         keyboard works comfortably on remotes. -->
    <style name="Theme.TvKiosk.Config" parent="Theme.AppCompat">
        <item name="android:windowBackground">#0F172A</item>
    </style>
</resources>
